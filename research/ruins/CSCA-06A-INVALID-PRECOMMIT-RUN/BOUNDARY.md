# CSCA-06A invalid precommit execution

An accidental invocation `python experiments/csca_06a_falsifiability/run.py --help` occurred after the import-path repair but before that repair/amendment had been committed. The runner has no argument parser, so it executed rather than printing help.

The resulting files are sealed here **without scientific interpretation** and are excluded from all claim metrics. The originally reserved 61000/71000 confirmatory seed cohorts are burned and may not be reused. A new pre-execution amendment assigns untouched 81000/91000 cohorts before the authoritative run.

Status: `INSTRUMENT_PROVENANCE_INVALID_PRECOMMIT_EXECUTION`.
