# CWC Main Hypothesis — Current Scientific State

Date: 2026-08-11

## Master question

Can a causally controlled CWC adaptive-computation architecture Pareto-dominate static, generic-dynamic, MoD and MoE alternatives under matched resource accounting on a fixed real-world workload panel, and reproduce that advantage under fresh replication?

## Current verdict split

### Executed mechanism branch

`CWC-FLAGSHIP-ROUTE-01 = NOT_SUPPORTED`.

The preregistered shared decision-relevant ridge router over the raw block-1 mean-hidden-state coordinate system passed `0/6` PRIMARY seed-family cells and `2/6` REPLICATION cells. PRIMARY failure is irreversible by the frozen rule. Architecture promotion from this branch is blocked.

### Architecture-level L7

`H-L7 = NOT_TESTED`.

The mechanism negative is not an architecture-wide impossibility result. Full L7 has not executed a fixed external panel with frozen base-model identity and matched static / generic-dynamic / MoD / MoE implementations.

## Why the mechanism failed

The result is not explained by absence of allocatable second-layer value.

A post-hoc, target-dependent exhaustive oracle diagnostic finds positive maximum quality advantage over the exact fixed-depth logical-FLOP frontier in all `12/12` seed-family cells. The maximum oracle advantages range from `0.0124020848` to `0.0217229631` cross-entropy with optimal continuation rates between `0.4375` and `0.53125`.

The frozen learned router instead produced continuation rates spanning `0.0` to `1.0` and failed the all-cell gate. Held-out predicted-gain correlations are unstable across independent model seeds.

A second non-promoting diagnostic refit the same ridge form separately in each model's own CALIBRATION representation. This raised cell PASS from `2/12` under the frozen shared policy to `6/12`, including all `3/3` PRIMARY CODE cells, but did not stabilize PROSE. Therefore cross-seed hidden-coordinate nonalignment is a real failure source, but not the sole source.

## Historical correction

The old WP18 programme stop remains historical evidence, but its interpretation is narrowed. WP17 measured router cost as a FLOP ratio while WP18 compared that number directly to a loss/utility-space lower confidence bound. That cost-to-utility bridge was not dimensionally established. Negative `G_lo` means the pilot failed to certify positive value; it does not prove the true gap is below routing cost.

`CWC-FLAGSHIP-ROUTE-01` removes that ambiguity by comparing quality directly at exact logical FLOP budgets, and it still returns `NOT_SUPPORTED` for the shared-router branch.

## Master-hypothesis repair

The historical H-L7 phrase “dominates on >=2 real workloads” is open-ended and permits workload hunting. `docs/methodology/L7_FIXED_PANEL_REVISION_2026-08-11.md` now operationally fixes the next admissible L7 panel to exactly AVeriTeC + HybridQA and forbids workload replacement after outcomes.

The new sequential gate is:

1. checksum-bound source/model/comparator provenance;
2. static fixed-frontier necessary condition;
3. matched generic-dynamic comparator;
4. matched MoD and MoE comparators;
5. fresh replication.

Any PRIMARY failure terminates the panel with `NOT_SUPPORTED`; replication cannot rescue it.

## Current blockers

L7-R1 cannot execute yet because the environment does not contain checksum-bound local external dataset snapshots, a frozen suitable model/tokenizer/runner manifest, or executable matched MoD/MoE comparator bindings. These are `NOT_TESTED` blockers, not evidence for or against L7.

## Current scientific truth

- Synthetic cognitive/governance primitives: several are narrowly supported under their frozen scopes.
- Formal identifiability substrate: qualified and mandatory in admission.
- Real internal shared-router adaptive-depth mechanism: `NOT_SUPPORTED`.
- General adaptive allocation opportunity: not killed; oracle headroom is present descriptively.
- Architecture-level CWC Pareto superiority: `NOT_TESTED`.
- External REAL-TRANSFER-01: `NOT_TESTED`.
- Architecture promotion: `BLOCKED` until a new preregistered candidate clears the fixed-panel sequence.

This boundary is intentionally asymmetric: a future positive must overcome the negative and all stronger baselines; the current negative may not be erased by changing router, workload or evaluation rule under the same experiment identity.
