# COG-COUNTERMODEL-01R — Execution Report

**Verdict:** `SET_VALUED_COUNTERMODEL_GUARD_QUALIFIED_SYNTHETIC_NARROWED`  
**Authority:** `COUNTERMODEL_SET_GUARD_ONLY`

## Parent negative preserved

`COG-COUNTERMODEL-01` remains `AUTONOMOUS_COUNTERMODEL_GENERATOR_NOT_QUALIFIED`. Its failed hidden-truth/Pareto recovery requirement was not weakened or retroactively reinterpreted as a PASS.

The repair changes the scientific target: the system must represent the observational equivalence set and block consolidation, not guess which observationally equivalent member generated the synthetic world.

## Confirmatory design

- PRIMARY: 64 fresh seeds/family (`91000..91063`).
- independent REPLICATION: 64 fresh seeds/family (`101000..101063`).
- 4096 factual rows per seed.
- material causal displacement `Delta_beta=0.40`.
- finite diagnostic grid `[-0.5,2.0]`, step `0.025`.
- declared direct-effect L2 bound `epsilon_eta=0.15` for assumption-conditional interval diagnostics.

## Results

For every eligible family (`VALID`, `COORDINATED_EXCLUSION`, `ALEATORIC_HIGH`) in both cohorts:

- unrestricted equivalence set classified as `ALL_REAL_BETA_UNDER_UNRESTRICTED_REPARAMETERIZATION`: `64/64`;
- materially distinct exact countermodel survival: `64/64`;
- finite-grid ambiguity diameter >= 1.0: `64/64`;
- non-empty Pareto frontier: `64/64`;
- maximum frontier path reconstruction error remained <= `3.553e-15`;
- unconditional causal authority count: `0`.

Under the declared direct-effect bound, VALID and COORDINATED_EXCLUSION both produced an analytic beta interval in `64/64` seeds, every interval width was below the material shift 0.40, and no materially shifted beta survived *inside that assumption-bound interval*. Median interval widths were approximately:

- PRIMARY VALID: `0.28905`;
- PRIMARY COORDINATED: `0.29067`;
- REPLICATION VALID: `0.29091`;
- REPLICATION COORDINATED: `0.29049`.

The runtime state remained `ASSUMPTION_CONDITIONAL_IDENTIFICATION_COUNTERMODELS_OUTSIDE_BOUNDS`, not causal truth.

For the upstream-invalid regime-confounding family, countermodel search was refused `64/64` in both cohorts (`UPSTREAM_CANDIDATE_NOT_ELIGIBLE`).

## Mathematical interpretation

The key result is set-valued, not point-valued. In the admitted reduced-form reparameterization class, factual fit alone permits a continuum of beta values. A structural bound can narrow this set, but that narrowing inherits the epistemic status of the bound.

This means the cognitive core now has an executable distinction between:

1. `no factual contradiction to many causal worlds`, and
2. `one causal world survives only because an explicit assumption excludes the others`.

The second state cannot be silently consolidated as environmental causal truth.

## Non-promotion boundary

Not established:

- semantic causality;
- validity of the structural bound on real traces;
- real-trace causal identification;
- replay benefit;
- active causal control;
- large-scale architecture promotion.

Next hard dependency: `COG-EPISTEMIC-01`, a typed epistemic lattice that makes these authority boundaries impossible to violate by string/status drift.
