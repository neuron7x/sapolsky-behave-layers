# CWC-INF-01 / Real-Model Replay Pilot — BLOCKED DRAFT

**Status:** `BLOCKED_BY_CSCA_02_UA_FAIL`  
**Execution authority:** NONE.

This artifact exists because ACT-R&D-03 requires the prospective real-model pilot boundary to be explicit. It is **not** an executable preregistration until a later human H4 record authorizes a new uncertainty mechanism that first passes a fresh counterfactual-authority qualification.

If unblocked by a new act, the pilot must freeze before execution:

- one real nanochat checkpoint hash and tokenizer hash;
- one primary model-internal intervention family;
- one primary outcome (e.g. next-token distribution divergence);
- matched base/shadow inference seeds;
- immutable candidate IDs;
- fixed replay-compute budgets;
- controls: resolution-aware debt, RPE proxy, recency, uncertainty-only, uniform, random;
- failure rule on false consolidation and OOS replay utility;
- physical replay-cost ledger.

Current CSCA-02-UA data cannot be reused as confirmatory calibration for the successor mechanism.
