# CSCA-05 Shadow Promotion Decision

`SHADOW_RUNTIME_PATH_QUALIFIED_NARROWED` is permitted only for the direct-intervention byte-level nanochat pilot.

It does **not** overwrite ACT-R&D-03's negative learned-world-model uncertainty verdict and does not authorize replay, logits, token sampling, weights, topology or active causal control.

The qualified path is:

`base inference -> immutable prompt trace -> direct model intervention probes -> finite-budget credit envelope -> context-bound ACCEPT/ABSTAIN -> audit log`.

Every accepted decision remains advisory and has `architecture_authority=false`.

The next promotion requires a new preregistration. Intervention-operator sensitivity and ~4x CPU shadow overhead are explicit blockers to broader runtime authority.
