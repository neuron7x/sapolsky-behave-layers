# WP16 — Clean-room Reproduction Results

**Act:** CWC-ASCEND-2026-01, G0/G1. **Commit:** `5a0924c`. **Class ceiling:** reproduction only —
raises no scientific claim tier (cannot support L7/L8).

## What was done
A fresh venv was built from `uv.lock --frozen` (gpu/cu128 extra) plus pinned `cwc-requirements-dev.txt`,
**independent of the author's `.venv`**, then the full canonical gate set was re-run with that
clean-room interpreter. This closes the Act §1 audit boundary: the capsule shipped without an exact
`.venv`, so the stored test/coverage/mutation numbers had never been independently re-executed.

Machine-readable detail (host, GPU, driver, CUDA, PyTorch, per-gate exit code + wall time, pytest
skip reason codes): **`reproduction_report.json`** in this bundle.

## Outcome (clean-room venv, torch 2.9.1+cu128, CUDA available)
Every non-hardware canonical gate reproduces PASS:
`lint · typecheck · test (211 passed, 2 skipped) · coverage (≥95% floor) · mutation (12/12 mutants
killed) · experiment-tests (full suite) · validate-evidence · doc-gate · reproduce-primary`.

`reproduce-primary` regenerated the primary verdict `SYNTHETIC_HALT_IDENTITY_VERIFIED` from scratch
(wp4 adaptive-depth, 8 seeds) inside the preregistered tolerance — the canonical evidence was not
modified.

## NOT_MEASURED (never PASS)
- **Energy / NVML power sampling** — `test_instrumentation_energy.py`: NVML power sampling not
  available on this host; `pynvml` is not a nanochat dependency. Graceful-degradation is covered by
  the facade-unavailable test. Recorded NOT_MEASURED, per Act WP16 acceptance.
- **Second independent host** — single-host environment. The Act's 3rd clean environment (an
  independent runner/host) is out of scope here and is recorded NOT_MEASURED; it does not count
  toward or against the verdict. Independent *replication by a different operator* is L8 (WP25),
  explicitly not attempted.

## Verdict
`CLEANROOM_REPRODUCTION_PASS` — the evidence reproduces from a clean checkout with a frozen-lock
environment and no author intervention. See `reproduction_report.json` for the authoritative record.

## Provenance regenerated at this commit (machine-derived, not hand-maintained)
`RELEASE_MANIFEST.json` (claims from `claim_registry.json`: 23 SUPPORTED / 4 NARROWED / 8
NOT_SUPPORTED / 2 NOT_TESTED; 48 evidence bundles), `SBOM.spdx.json` (106 packages from `uv.lock`),
`CITATION.cff` (re-stamped). Generated by `scripts/wp16_release_spine.py`.
