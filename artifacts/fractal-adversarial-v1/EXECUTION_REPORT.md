# CWC FRACTAL ADVERSARIAL V1 — EXECUTION REPORT

**Final machine verdict:** `ADVERSARIAL_BOUNDARIES_ESTABLISHED_NO_ASCENSION`  
**VIA authority:** `false`  
**Scientific ascension authority:** `false`

## 1. Objective

Attack the legacy multiscale/fractal diagnostics and archived CWK adaptive-compute semantics through independent evidence modalities: prospective traces, statistical nulls, synthetic controls, Monte Carlo estimator calibration, exact topology analysis, static implementation semantics, and dynamic execution hooks. The programme is deliberately failure-seeking; it may narrow or falsify claims but cannot promote VIA-V2+.

## 2. Protocol / provenance

- `CWC-FRACTAL-ADV-01`: fresh-trace robust multiscale and execution-reality protocol; frozen before fresh traces.
- `CWC-FRACTAL-ADV-02`: one-way cross-seed replication veto; explicitly post-hoc and therefore forbidden from upgrading evidence.
- `CWC-FRACTAL-ADV-03`: topology semantics and multi-seed/shape execution matrix; frozen before matrix execution.
- Prespecified trace/model seeds: `101, 211, 307`.
- Archived runtime source: `legacy/cognitive-weave-kernel-archive.zip`.
- A failed execution-matrix attempt caused by the archived attention-density contract is preserved as `FAILED_RUN_001_EXECUTION_MATRIX.md`; the audit-only normal path was then made legal with `max_attention_density=1.0`, while the dedicated failing-attention probe remained unchanged in purpose.

## 3. Computation ledger

- Fresh prospective CWK trace records: **408** (3 seeds × fixed 64 + variable 72).
- Fresh variable-trace null evaluation: **3 × 2,000** Monte-Carlo iterations, each over three null families.
- Synthetic calibration: **3 controls × 2,000** iterations; endogenous positive control passed, independent and common-driver controls failed as required.
- Post-hoc pooled replication diagnostic: **5,000** iterations.
- Legacy estimator Monte Carlo: **60,000 simulated series**, yielding 4 metrics each (**240,000 metric values**).
- Dynamic execution matrix: **144** seed×shape×mode×budget cells, plus 12 active-expert comparisons, 12 failing-attention probes and 12 memory-order probes.
- Topology scaling audit: **11** sequence lengths from 8 to 256.

## 4. Statistical multiscale attack

- seed 101: residual coherence `0.190250`, family-wise p `0.134433`, delta vs max-null mean `0.046671` → `RESIDUAL_MULTISCALE_ORGANIZATION_NOT_SUPPORTED`.
- seed 211: residual coherence `0.275400`, family-wise p `0.029485`, delta vs max-null mean `0.110252` → `TRACE_STATISTICAL_GATE_PASS_PENDING_SYNTHETIC_CONTROL_CALIBRATION`.
- seed 307: residual coherence `0.150930`, family-wise p `0.735632`, delta vs max-null mean `-0.036221` → `RESIDUAL_MULTISCALE_ORGANIZATION_NOT_SUPPORTED`.

Only **1/3** prespecified variable-shape seeds passed the frozen per-trace gate. Therefore the one-way replication veto fires: `CROSS_SEED_RESIDUAL_MULTISCALE_ORGANIZATION_NOT_REPLICATED`.

The exploratory pooled statistic is nontrivial (`observed=0.205527`, family-wise `p=0.036793`), but its delta over the max-null mean is only `0.057612` and the synthesis rule was defined after inspecting per-trace results. It therefore cannot rescue failed replication or become confirmatory evidence.

### Cross-seed pair behavior

- `expert_utilization_entropy → end_to_end_p95_ms`: 101:-0.298, 211:-0.127, 307:+0.059; sign consistency `0.667`.
- `operational_intensity → total_flops`: residual variance collapsed in all seeds; not an endogenous association.
- `controller_depth_fraction → controller_memory_read_fraction`: 101:-0.123, 211:+0.681, 307:+0.065; sign consistency `0.667`.
- `route_entropy_mean → expert_utilization_entropy`: 101:-0.150, 211:-0.018, 307:-0.329; sign consistency `1.000`.

The strongest raw `operational_intensity → total_flops` association is `ρ≈+0.874126` in every variable-shape seed, but exact `(batch_size, sequence_length)` residualization drives both residual trajectories to degeneracy. This is direct evidence of a shared shape driver, not cross-scale organization.

All three fixed-shape traces retain only 2/4 non-degenerate prespecified pairs, below the frozen 75% coverage requirement, so **0/3** fixed traces are identifiable under this instrument. The correct result is instrumentation insufficiency, not a biological/architectural negative.

## 5. Synthetic controls

- `endogenous`: residual coherence `0.986866`, family-wise p `0.000500`, gate `True`.
- `common_driver`: residual coherence `0.065552`, family-wise p `0.977511`, gate `False`.
- `independent`: residual coherence `0.068553`, family-wise p `0.971514`, gate `False`.

Verdict: `SYNTHETIC_CONTROL_CALIBRATION_PASS`. The v2 statistic can detect a deliberately endogenous signal while rejecting independent noise and a strong common-driver-only construction. This validates the **gate behavior**, not CWK cognition.

## 6. Estimator calibration attack

- `iid_gaussian` — n=32: H=0.633 [0.408,0.844]; n=64: H=0.620 [0.462,0.767]; n=128: H=0.604 [0.489,0.715].
- `ar1_phi_0p8` — n=32: H=0.883 [0.679,1.027]; n=64: H=0.881 [0.733,0.986]; n=128: H=0.863 [0.754,0.950].
- `random_walk` — n=32: H=0.924 [0.741,1.050]; n=64: H=0.948 [0.826,1.022]; n=128: H=0.962 [0.875,1.016].
- `linear_trend_plus_noise` — n=32: H=0.721 [0.494,0.913]; n=64: H=0.725 [0.570,0.853]; n=128: H=0.736 [0.637,0.820].

For IID Gaussian noise, the legacy R/S Hurst estimator has mean `0.633` at n=32 and `0.604` at n=128 rather than the asymptotic IID reference 0.5. The legacy box-counting mean changes from `0.976` to `1.531` solely as series length changes. The binary sign-entropy diagnostic stays near one across all process classes. These finite-sample diagnostics are therefore retained as descriptive probes only, never as claim-bearing evidence of fractality.

## 7. Topology / graph-fractality attack

The archived smoke topology has `local_window=3`, `global_tokens=1` and causal masking. The exact global-token edge formula matches the runtime at **all 11** tested lengths.

- edge-count log-log slope over the finite tested range: `1.068534` (exact formula is asymptotically O(n));
- density log-log slope: `-0.931466` (asymptotically O(1/n));
- undirected projection diameter: **2 at every tested scaling length** because global token 0 is adjacent to all later tokens.

A graph-distance box-scaling claim therefore has no usable distance-scale range in this reference topology. Sparse linear wiring is not equivalent to fractal geometry. Verdict: `GRAPH_DISTANCE_FRACTALITY_NOT_IDENTIFIABLE_GLOBAL_HUB_COLLAPSES_SCALE`.

## 8. Physical execution attack

- Gate-changing active/depth trials: **96**.
- Trials with reduced expert rows: **0**.
- `max_active_experts` comparisons: **12**, route effects: **0**.
- Deliberately failing attention-budget probes: **12**, attention executed first in **12**.
- Memory probes received all query rows before controller memory gating: `True`.

This dynamically confirms the source-order semantics: expert updates are computed before active/depth multiplication; attention density is checked after dense `nn.MultiheadAttention`; legal `max_active_experts` values above `top_k` do not alter routing; and per-token memory gating occurs after retrieval. The archived runtime therefore demonstrates semantic gating/telemetry, **not physical conditional execution**.

## 9. Final epistemic verdict

`ADVERSARIAL_BOUNDARIES_ESTABLISHED_NO_ASCENSION`

- `attention_budget_is_preexecution_governor` = `false`
- `controller_memory_gate_precedes_retrieval_work` = `false`
- `graph_distance_fractality_supported` = `false`
- `max_active_experts_is_effective_governor_above_top_k` = `false`
- `multiscale_replication_supported` = `false`
- `physical_conditional_execution_supported` = `false`
- `short_series_estimator_calibration_complete` = `true`
- `short_series_fractal_estimators_claim_authorized` = `false`
- `synthetic_statistical_gate_calibrated` = `true`

The strongest surviving positive result is methodological: the hardened statistical gate passes its synthetic endogenous control and rejects its two negative controls. The strongest architecture result is negative: the archived CWK controller changes semantic masks without reducing expert work in 96/96 gate-changing matrix trials.

## 10. What remains scientifically open

1. A new prospective, independent trace campaign could test the one cross-seed-stable candidate association (`route_entropy_mean → expert_utilization_entropy`) without using the already-inspected seeds for confirmation.
2. A new runtime must move selection **before** expensive expert/attention/memory work and prove skipped kernels/rows physically, preferably on GPU with device traces; the archived reference cannot satisfy VIA-V4.
3. If graph fractality remains a research hypothesis, the topology must provide a genuine multi-scale graph-distance regime and a preregistered box-covering/null framework; the current global-hub graph cannot.
4. VIA remains governed by its own fail-closed DAG. None of this sidecar evidence authorizes VIA-V2+ or overrides the existing VIA-V1 boundary.

## 11. Reproducibility boundary

All result files are machine-readable and are bound by `SHA256SUMS`. CPU timing is diagnostic only. No GPU speedup, biological equivalence, intelligence claim, or general absence of multiscale organization is inferred.
