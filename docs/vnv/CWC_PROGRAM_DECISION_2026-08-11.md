# CWC Programme Decision — 2026-08-11

Status: `CONTINUE_NARROWED / ADAPTIVE-DEPTH LINE TERMINATED`

## Decision

Do **not** terminate the entire CWC repository/research programme. Terminate the current learned
adaptive-depth architecture-promotion lineage.

This is not a compromise verdict. It separates two independent scientific objects that the
repository previously carried under one umbrella:

1. **adaptive-depth Pareto architecture line** — now has two direct real-data negatives, including
   the preregistered final R2 rescue; this line is closed and receives no R3;
2. **decision-equivalence / decision-relevant epistemic-control line** — has narrow synthetic
   support but its external fixed-panel experiment (`REAL-TRANSFER-01`) remains `NOT_TESTED` and does
   not depend on the killed two-exit router.

Killing the whole project because object (1) failed would falsely transfer a negative across
mechanistically independent hypotheses. Continuing object (1) would violate the preregistered kill
rule. Therefore the only coherent action is **programme narrowing**.

## Evidence forcing termination of adaptive-depth lineage

- R1 shared router: PRIMARY `0/6`, REPLICATION `2/6` -> `NOT_SUPPORTED`.
- R1 post-hoc same-model calibration identified a concrete cross-seed coordinate failure and gave
  exactly one justified rescue mechanism.
- R2 froze that rescue before new outputs: each independently trained model receives only its own
  CALIBRATION-only ridge router; model/training/FLOP/frontier/baseline rules otherwise remain fixed.
- R2: PRIMARY `5/6`, REPLICATION `4/6` -> `CWC_FLAGSHIP_ROUTE_02_NOT_SUPPORTED`.
- CODE passes `6/6`; PROSE passes only `3/6`.
- Independent recomputation reproduces the verdict; verifier mutations killed `5/5`.

The repair improved performance but failed robustness. The remaining failure is no longer explained
by the original cross-seed coordinate defect alone.

## Sole surviving scientific flagship

CWC now has one promotion path:

> Preserve causal ambiguity when surviving worlds are decision-equivalent; acquire evidence only
> against distinctions capable of changing the decision; abstain when the decision remains
> unidentified.

Promotion requires `REAL-TRANSFER-01` on the frozen AVeriTeC + HybridQA external families under the
already defined source/model manifests, safety endpoints, direct comparators and fresh replication.
No internal synthetic result can substitute for that execution.

## Final kill rule for the scientific flagship

Once `REAL-TRANSFER-01` becomes executable:

- any frozen PRIMARY family endpoint failure => external flagship `NOT_SUPPORTED`;
- replication cannot rescue PRIMARY;
- failure closes the current decision-relevant flagship claim rather than spawning threshold,
  workload or feature search;
- if that occurs, CWC remains useful engineering/research infrastructure, but **the current claim of
  an original empirically useful cognitive-control mechanism is terminated**.

Until external bytes/model identity are materialized, that flagship is `NOT_TESTED`, not supported.

## Operational consequences

- no more work on `CWC-FLAGSHIP-ROUTE-01/02` except verification/archival maintenance;
- no L7 architecture-promotion language from adaptive-depth evidence;
- L5/L6/L7 remain blocked rather than filled with replacement mechanisms;
- engineering effort goes only to making `REAL-TRANSFER-01` executable and independently
  reproducible;
- a failure there ends the current scientific flagship.
