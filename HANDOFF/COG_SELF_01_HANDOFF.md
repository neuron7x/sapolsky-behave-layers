# COG-SELF-01 HANDOFF

## Qualified result

`AUTONOMOUS_DECISION_RELEVANT_FALSIFICATION_GOVERNOR_QUALIFIED_SYNTHETIC_NARROWED`

Authority: `SYNTHETIC_SELF_FALSIFICATION_SAFETY_SELECTION_PRIMITIVE_ONLY`.

## Executable cognitive path

`typed epistemic authority -> dependency-bound memory -> admitted worlds -> proof-carrying plan -> action-equivalence partition -> certified falsification attack -> monotone-negative update`

The governor asks a narrow operational question: which currently admissible test has the
lowest necessary certified cost against the alternatives that can actually flip the live
decision? Same-decision causal ambiguity stays unresolved and cannot create spend by
itself.

## Critical invariants

- a stale or altered plan certificate cannot authorize an attack;
- attack targets must belong to the transitive load-bearing memory/assumption graph;
- uncertified or incomplete information rates cannot authorize spend;
- if all worlds imply the same robust decision, self-falsification spend for that
  immediate decision is zero even when a cheap high-information probe exists;
- `SURVIVED` and `INCONCLUSIVE` cannot promote epistemic authority;
- negative outcomes can only retract a bound memory or invalidate a bound assumption;
- dependency retractions propagate through the existing ledger;
- no runtime API in COG-SELF-01 can rewrite frozen evidence or mint stronger causal
  authority.

## Confirmatory evidence

PRIMARY and fresh REPLICATION each executed 12 families x 128 cases = 1536 cases.
Every family passed 128/128. In each cohort: zero false spend, zero stale-plan
acceptance, zero irrelevant/uncertified attack selection, zero permutation disagreement,
zero survival promotion, zero target-boundary violation, zero stale/unbound outcome
acceptance, and 256/256 negative-propagation checks passed. Generation and replay hashes
matched exactly. Semantic gate: 8/8 mutations killed.

## Boundary

This qualifies a synthetic selection/update safety primitive, not autonomous science.
World completeness, utilities, attack generation and certified information-rate validity
remain external inputs. No real-model transfer, natural-language robustness, production
control, matched-compute advantage, causal truth or external reproduction follows.

## Next hard gate

The internal synthetic cognitive-core chain is no longer the dominant uncertainty. Move
to external validity: a public/matched-interface CAB evaluation with independently
authored instances, contamination controls, strong act/abstain/query/reject baselines and
at least two non-synthetic or real-model task families. Primary comparison: lower false
causal authority and lower wasted acquisition without lower decision utility. Flagship
promotion still requires independent third-party reproduction.
