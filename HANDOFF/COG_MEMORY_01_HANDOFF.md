# COG-MEMORY-01 HANDOFF

## Authoritative result

`ASSUMPTION_AWARE_MEMORY_CONSOLIDATION_QUALIFIED_SYNTHETIC_NARROWED`

Authority: `EPISTEMIC_MEMORY_PRIMITIVE_ONLY`.

## What exists

- `cwc/memory/epistemic_store.py`: immutable epistemic-memory records, version history,
  countermodel veto, dependency closure, assumption invalidation, hash-chained events.
- `experiments/cog_memory_01/PREREGISTRATION.md`: frozen design.
- `experiments/cog_memory_01/run.py`: PRIMARY/REPLICATION confirmatory runner.
- `scripts/cog_memory01_gate.py`: result gate + six semantic mutations.
- `artifacts/cog-memory-01/`: sealed matrix/verdict/checksums.
- `docs/COG_MEMORY_01_THEORY.md`: exact authority boundary.

## Frozen result

PRIMARY and REPLICATION each passed all 12 families at 128/128, with zero false causal
consolidation, zero invariant/event-chain failures, full transitive retraction, zero
tampered/legacy acceptance and no in-place authority upgrade.

## Non-promotion boundary

Do not infer semantic causality, replay benefit, planning value, active-control safety,
production readiness, or external replication.

## Next hard gate

`COG-PLAN-01 — Proof-Carrying Counterfactual Planning`.

The planner should consume the typed memory ledger and maintain branches over surviving
countermodels. A plan is admissible only when its action ranking is robust across the
allowed world set or explicitly assumption-conditional. It must never average
incompatible worlds into hidden certainty. Preregister the branch semantics,
robust-decision metric, countermodel stress families, budget rule and abstention states
before implementation or confirmatory evaluation.
